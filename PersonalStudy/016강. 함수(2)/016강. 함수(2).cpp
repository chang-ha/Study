#include <iostream>

int main()
{
	// 가장 먼저 실행되는 함수는 main
	// 프로그램이 종료될 때 == main이 종료될 때
	// 함수 동작방식 << main이 끝나기 전에는 함수가 절대 먼저 끝날 수 없음
	// 스택과 유사한 형식 (선입후출, 먼저 들어온애가 나중에 나간다)
	// 함수가 사용하는 메모리 공간 == 스택 메모리 영역
	// 메모리 영역 != 코드
	// 함수, 내가 작성한 코드 == 명령어(작업지시서)
	// 해당 작업지시서를 보고 스택영역에서 직접적으로 작업이 진행됨
}

