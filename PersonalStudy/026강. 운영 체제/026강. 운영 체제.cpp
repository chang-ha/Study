// 026강. 운영 체제


#include <iostream>
int main()
{
	// 플랫폼
	// 무언가 실행될 수 있는 기반
	// ex) 윈도우, 스팀 등...
	// 스팀에서 산 게임을 에픽게임즈에서 실행할 순 없다

	// visual studio에서 말하는 플랫폼이란??
	// 윈도우는 32비트와 64비트가 있었다.
	// 너가 실행한 프로젝트가 32비트 운영체제에서 실행될지??? 아님 64비트 운영체제에서 실행될지??? 정해라
	// 왜 32bit는 x86이야?? << 그 당시 Cpu이름이 x86이 있어서 이게 굳어지듯이 사용되어서 32bit가 x86이 되어버림 << 카더라통신일 가능성있음
	// 32bit == 데이터 처리 단위가 4바이트
	// 64bit == 데이터 처리 단위가 8바이트
}

